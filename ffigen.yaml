# Run with `dart run ffigen --config ffigen.yaml`.
name: NitroWebgpuBindings
description: |
  Bindings for `src/nitro_webgpu.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/nitro_webgpu_bindings_generated.dart'
headers:
  entry-points:
    - 'src/nitro_webgpu.h'
  include-directives:
    - 'src/nitro_webgpu.h'
comments:
  style: any
  length: full
