# Optimized Nitrogen configuration.
# Restricts the generator to lib/src/**/*.native.dart for faster builds.
targets:
  $default:
    builders:
      nitro_generator:
        generate_for:
          - lib/src/**.native.dart
