// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.5.12
// Generated from: nitro_webgpu.native.dart
package nitro.nitro_webgpu_module

import android.app.Activity
import android.content.Context
import androidx.annotation.Keep
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.launch
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.CoroutineStart
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.flow.buffer
import kotlinx.coroutines.runBlocking

// --- @HybridRecord Kotlin data classes (generated by Nitrogen) ---

@androidx.annotation.Keep
data class GpuInstanceOptions(val backends: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): GpuInstanceOptions {
            val backends = buf.long
            return GpuInstanceOptions(backends)
        }
        @JvmStatic fun decode(bytes: ByteArray): GpuInstanceOptions {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): GpuInstanceOptions = GpuInstanceOptions(
                backends = (map["backends"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(8) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(backends.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, GpuInstanceOptions> support ---
    fun toJson(): Map<String, Any?> = mapOf("backends" to backends)

}

@androidx.annotation.Keep
data class GpuRequestAdapterOptions(val powerPreference: Long, val forceFallbackAdapter: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): GpuRequestAdapterOptions {
            val powerPreference = buf.long
            val forceFallbackAdapter = (buf.get().toInt() != 0)
            return GpuRequestAdapterOptions(powerPreference, forceFallbackAdapter)
        }
        @JvmStatic fun decode(bytes: ByteArray): GpuRequestAdapterOptions {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): GpuRequestAdapterOptions = GpuRequestAdapterOptions(
                powerPreference = (map["powerPreference"] as Number).toLong(),
                forceFallbackAdapter = map["forceFallbackAdapter"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(powerPreference.toLong())
        writeBool(forceFallbackAdapter)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, GpuRequestAdapterOptions> support ---
    fun toJson(): Map<String, Any?> = mapOf("powerPreference" to powerPreference, "forceFallbackAdapter" to forceFallbackAdapter)

}

@androidx.annotation.Keep
data class GpuAdapterInfo(val vendor: String, val architecture: String, val device: String, val description: String, val backendType: Long, val adapterType: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): GpuAdapterInfo {
            val vendor = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val architecture = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val device = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val description = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val backendType = buf.long
            val adapterType = buf.long
            return GpuAdapterInfo(vendor, architecture, device, description, backendType, adapterType)
        }
        @JvmStatic fun decode(bytes: ByteArray): GpuAdapterInfo {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): GpuAdapterInfo = GpuAdapterInfo(
                vendor = map["vendor"] as String,
                architecture = map["architecture"] as String,
                device = map["device"] as String,
                description = map["description"] as String,
                backendType = (map["backendType"] as Number).toLong(),
                adapterType = (map["adapterType"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(160) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(vendor)
        writeString(architecture)
        writeString(device)
        writeString(description)
        writeInt(backendType.toLong())
        writeInt(adapterType.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, GpuAdapterInfo> support ---
    fun toJson(): Map<String, Any?> = mapOf("vendor" to vendor, "architecture" to architecture, "device" to device, "description" to description, "backendType" to backendType, "adapterType" to adapterType)

}

@androidx.annotation.Keep
data class GpuLimits(val maxTextureDimension1D: Long, val maxTextureDimension2D: Long, val maxTextureDimension3D: Long, val maxTextureArrayLayers: Long, val maxBindGroups: Long, val maxBindingsPerBindGroup: Long, val maxUniformBufferBindingSize: Long, val maxStorageBufferBindingSize: Long, val minUniformBufferOffsetAlignment: Long, val minStorageBufferOffsetAlignment: Long, val maxBufferSize: Long, val maxComputeWorkgroupStorageSize: Long, val maxComputeInvocationsPerWorkgroup: Long, val maxComputeWorkgroupSizeX: Long, val maxComputeWorkgroupSizeY: Long, val maxComputeWorkgroupSizeZ: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): GpuLimits {
            val maxTextureDimension1D = buf.long
            val maxTextureDimension2D = buf.long
            val maxTextureDimension3D = buf.long
            val maxTextureArrayLayers = buf.long
            val maxBindGroups = buf.long
            val maxBindingsPerBindGroup = buf.long
            val maxUniformBufferBindingSize = buf.long
            val maxStorageBufferBindingSize = buf.long
            val minUniformBufferOffsetAlignment = buf.long
            val minStorageBufferOffsetAlignment = buf.long
            val maxBufferSize = buf.long
            val maxComputeWorkgroupStorageSize = buf.long
            val maxComputeInvocationsPerWorkgroup = buf.long
            val maxComputeWorkgroupSizeX = buf.long
            val maxComputeWorkgroupSizeY = buf.long
            val maxComputeWorkgroupSizeZ = buf.long
            return GpuLimits(maxTextureDimension1D, maxTextureDimension2D, maxTextureDimension3D, maxTextureArrayLayers, maxBindGroups, maxBindingsPerBindGroup, maxUniformBufferBindingSize, maxStorageBufferBindingSize, minUniformBufferOffsetAlignment, minStorageBufferOffsetAlignment, maxBufferSize, maxComputeWorkgroupStorageSize, maxComputeInvocationsPerWorkgroup, maxComputeWorkgroupSizeX, maxComputeWorkgroupSizeY, maxComputeWorkgroupSizeZ)
        }
        @JvmStatic fun decode(bytes: ByteArray): GpuLimits {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): GpuLimits = GpuLimits(
                maxTextureDimension1D = (map["maxTextureDimension1D"] as Number).toLong(),
                maxTextureDimension2D = (map["maxTextureDimension2D"] as Number).toLong(),
                maxTextureDimension3D = (map["maxTextureDimension3D"] as Number).toLong(),
                maxTextureArrayLayers = (map["maxTextureArrayLayers"] as Number).toLong(),
                maxBindGroups = (map["maxBindGroups"] as Number).toLong(),
                maxBindingsPerBindGroup = (map["maxBindingsPerBindGroup"] as Number).toLong(),
                maxUniformBufferBindingSize = (map["maxUniformBufferBindingSize"] as Number).toLong(),
                maxStorageBufferBindingSize = (map["maxStorageBufferBindingSize"] as Number).toLong(),
                minUniformBufferOffsetAlignment = (map["minUniformBufferOffsetAlignment"] as Number).toLong(),
                minStorageBufferOffsetAlignment = (map["minStorageBufferOffsetAlignment"] as Number).toLong(),
                maxBufferSize = (map["maxBufferSize"] as Number).toLong(),
                maxComputeWorkgroupStorageSize = (map["maxComputeWorkgroupStorageSize"] as Number).toLong(),
                maxComputeInvocationsPerWorkgroup = (map["maxComputeInvocationsPerWorkgroup"] as Number).toLong(),
                maxComputeWorkgroupSizeX = (map["maxComputeWorkgroupSizeX"] as Number).toLong(),
                maxComputeWorkgroupSizeY = (map["maxComputeWorkgroupSizeY"] as Number).toLong(),
                maxComputeWorkgroupSizeZ = (map["maxComputeWorkgroupSizeZ"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(128) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(maxTextureDimension1D.toLong())
        writeInt(maxTextureDimension2D.toLong())
        writeInt(maxTextureDimension3D.toLong())
        writeInt(maxTextureArrayLayers.toLong())
        writeInt(maxBindGroups.toLong())
        writeInt(maxBindingsPerBindGroup.toLong())
        writeInt(maxUniformBufferBindingSize.toLong())
        writeInt(maxStorageBufferBindingSize.toLong())
        writeInt(minUniformBufferOffsetAlignment.toLong())
        writeInt(minStorageBufferOffsetAlignment.toLong())
        writeInt(maxBufferSize.toLong())
        writeInt(maxComputeWorkgroupStorageSize.toLong())
        writeInt(maxComputeInvocationsPerWorkgroup.toLong())
        writeInt(maxComputeWorkgroupSizeX.toLong())
        writeInt(maxComputeWorkgroupSizeY.toLong())
        writeInt(maxComputeWorkgroupSizeZ.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, GpuLimits> support ---
    fun toJson(): Map<String, Any?> = mapOf("maxTextureDimension1D" to maxTextureDimension1D, "maxTextureDimension2D" to maxTextureDimension2D, "maxTextureDimension3D" to maxTextureDimension3D, "maxTextureArrayLayers" to maxTextureArrayLayers, "maxBindGroups" to maxBindGroups, "maxBindingsPerBindGroup" to maxBindingsPerBindGroup, "maxUniformBufferBindingSize" to maxUniformBufferBindingSize, "maxStorageBufferBindingSize" to maxStorageBufferBindingSize, "minUniformBufferOffsetAlignment" to minUniformBufferOffsetAlignment, "minStorageBufferOffsetAlignment" to minStorageBufferOffsetAlignment, "maxBufferSize" to maxBufferSize, "maxComputeWorkgroupStorageSize" to maxComputeWorkgroupStorageSize, "maxComputeInvocationsPerWorkgroup" to maxComputeInvocationsPerWorkgroup, "maxComputeWorkgroupSizeX" to maxComputeWorkgroupSizeX, "maxComputeWorkgroupSizeY" to maxComputeWorkgroupSizeY, "maxComputeWorkgroupSizeZ" to maxComputeWorkgroupSizeZ)

}

@androidx.annotation.Keep
data class GpuDeviceDescriptor(val label: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): GpuDeviceDescriptor {
            val label = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return GpuDeviceDescriptor(label)
        }
        @JvmStatic fun decode(bytes: ByteArray): GpuDeviceDescriptor {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): GpuDeviceDescriptor = GpuDeviceDescriptor(
                label = map["label"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(36) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(label)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, GpuDeviceDescriptor> support ---
    fun toJson(): Map<String, Any?> = mapOf("label" to label)

}

@androidx.annotation.Keep
data class GpuError(val type: Long, val message: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): GpuError {
            val type = buf.long
            val message = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return GpuError(type, message)
        }
        @JvmStatic fun decode(bytes: ByteArray): GpuError {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): GpuError = GpuError(
                type = (map["type"] as Number).toLong(),
                message = map["message"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(44) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(type.toLong())
        writeString(message)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, GpuError> support ---
    fun toJson(): Map<String, Any?> = mapOf("type" to type, "message" to message)

}

@androidx.annotation.Keep
data class GpuUncapturedError(val deviceAddress: Long, val type: Long, val message: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): GpuUncapturedError {
            val deviceAddress = buf.long
            val type = buf.long
            val message = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return GpuUncapturedError(deviceAddress, type, message)
        }
        @JvmStatic fun decode(bytes: ByteArray): GpuUncapturedError {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): GpuUncapturedError = GpuUncapturedError(
                deviceAddress = (map["deviceAddress"] as Number).toLong(),
                type = (map["type"] as Number).toLong(),
                message = map["message"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(52) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(deviceAddress.toLong())
        writeInt(type.toLong())
        writeString(message)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, GpuUncapturedError> support ---
    fun toJson(): Map<String, Any?> = mapOf("deviceAddress" to deviceAddress, "type" to type, "message" to message)

}

@androidx.annotation.Keep
data class GpuDeviceLost(val deviceAddress: Long, val reason: Long, val message: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): GpuDeviceLost {
            val deviceAddress = buf.long
            val reason = buf.long
            val message = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return GpuDeviceLost(deviceAddress, reason, message)
        }
        @JvmStatic fun decode(bytes: ByteArray): GpuDeviceLost {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): GpuDeviceLost = GpuDeviceLost(
                deviceAddress = (map["deviceAddress"] as Number).toLong(),
                reason = (map["reason"] as Number).toLong(),
                message = map["message"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(52) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(deviceAddress.toLong())
        writeInt(reason.toLong())
        writeString(message)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, GpuDeviceLost> support ---
    fun toJson(): Map<String, Any?> = mapOf("deviceAddress" to deviceAddress, "reason" to reason, "message" to message)

}

@androidx.annotation.Keep
data class NitroNullableInt(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableInt {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroNullableInt(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableInt {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableInt = NitroNullableInt(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableInt> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableDouble(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableDouble {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroNullableDouble(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableDouble {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableDouble = NitroNullableDouble(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableDouble> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroNullableBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableBool = NitroNullableBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptInt64(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptInt64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroOptInt64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptInt64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptInt64 = NitroOptInt64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptInt64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptFloat64(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptFloat64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroOptFloat64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptFloat64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptFloat64 = NitroOptFloat64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptFloat64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroOptBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptBool = NitroOptBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

/**
 * Contract for the [NitroWebgpu] module.
 * Implement this in your Kotlin source code.
 * Nitro may call this implementation from any JNI thread.
 * Keep mutable state thread-safe or marshal work onto your own dispatcher.
 */
interface HybridNitroWebgpuSpec {
    val applicationContext: Context get() = NitroWebgpuJniBridge.applicationContext
    val activity: Activity? get() = NitroWebgpuJniBridge.activity

    // Optional lifecycle hooks — override only what you need.
    fun onAttached() {}
    fun onDetached() {}
    fun onActivityAttached(activity: Activity) {}
    fun onActivityDetached() {}

    // source: nitro_webgpu.native.dart:175
    fun initInstance(options: GpuInstanceOptions): Unit
    // source: nitro_webgpu.native.dart:178
    fun wgpuVersion(): String
    // source: nitro_webgpu.native.dart:185
    suspend fun requestAdapter(options: GpuRequestAdapterOptions): Long
    // source: nitro_webgpu.native.dart:187
    fun adapterGetInfo(adapter: Long): GpuAdapterInfo
    // source: nitro_webgpu.native.dart:188
    fun adapterGetLimits(adapter: Long): GpuLimits
    // source: nitro_webgpu.native.dart:189
    fun adapterRelease(adapter: Long): Unit
    // source: nitro_webgpu.native.dart:196
    suspend fun requestDevice(adapter: Long, descriptor: GpuDeviceDescriptor): Long
    // source: nitro_webgpu.native.dart:199
    fun deviceGetQueue(device: Long): Long
    // source: nitro_webgpu.native.dart:204
    fun deviceDestroy(device: Long): Unit
    // source: nitro_webgpu.native.dart:206
    fun deviceRelease(device: Long): Unit
    // source: nitro_webgpu.native.dart:207
    fun queueRelease(queue: Long): Unit
    // source: nitro_webgpu.native.dart:213
    fun devicePushErrorScope(device: Long, filter: Long): Unit
    // source: nitro_webgpu.native.dart:217
    suspend fun devicePopErrorScope(device: Long): GpuError?
    val uncapturedErrors: Flow<GpuUncapturedError>
    val deviceLostEvents: Flow<GpuDeviceLost>
}

@Keep
object NitroWebgpuJniBridge {
    private val _implementations = java.util.concurrent.ConcurrentHashMap<Long, HybridNitroWebgpuSpec>()
    private val _idCounter = java.util.concurrent.atomic.AtomicLong(0)
    private var _factory: (() -> HybridNitroWebgpuSpec)? = null
    private val _asyncExecutor = java.util.concurrent.Executors.newCachedThreadPool()

    lateinit var applicationContext: Context
        private set

    var activity: Activity? = null
        private set

    @JvmStatic external fun initialize(bridgeClass: Class<*>)

    fun registerFactory(factory: () -> HybridNitroWebgpuSpec, context: Context) {
        applicationContext = context
        _factory = factory
        if (_implementations.isEmpty()) { initialize(this::class.java) }
    }

    @JvmStatic
    fun create_instance_call(key: String): Long {
        val factory = _factory ?: throw IllegalStateException(
            "NitroWebgpu: no factory registered. Call registerFactory() in onAttachedToEngine().")
        val id = _idCounter.getAndIncrement()
        val impl = factory()
        _implementations[id] = impl
        impl.onAttached()
        return id
    }

    @JvmStatic
    fun destroy_instance_call(instanceId: Long) {
        _implementations.remove(instanceId)?.onDetached()
    }

    fun onActivityAttached(newActivity: Activity) {
        activity = newActivity
        _implementations.values.forEach { it.onActivityAttached(newActivity) }
    }

    fun onActivityDetached() {
        activity = null
        _implementations.values.forEach { it.onActivityDetached() }
    }

    // @NitroNativeAsync helpers — post primitive results via Dart_PostCObject_DL.
    @JvmStatic external fun postNullToPort(dartPort: Long)
    @JvmStatic external fun postInt64ToPort(dartPort: Long, value: Long)
    @JvmStatic external fun postDoubleToPort(dartPort: Long, value: Double)
    @JvmStatic external fun postBoolToPort(dartPort: Long, value: Boolean)
    @JvmStatic external fun postStringToPort(dartPort: Long, value: String)
    // Nullable prim helpers: malloc NitroOptXxx on native heap, post address as kInt64.
    // Dart decodes via Pointer<NitroOptXxx>.fromAddress and frees via the <lib>_nitro_free export.
    @JvmStatic external fun postOptInt64ToPort(dartPort: Long, value: Long, hasValue: Boolean)
    @JvmStatic external fun postOptFloat64ToPort(dartPort: Long, value: Double, hasValue: Boolean)
    @JvmStatic external fun postOptBoolToPort(dartPort: Long, value: Boolean, hasValue: Boolean)
    // Record/list-record helper: malloc a [4B len][payload] native buffer, copy
    // value into it, post the address as kInt64. Dart decodes via Pointer<Uint8>.
    // null posts address 0 (nullptr) — NOT Dart_CObject_kNull — matching how
    // nullable record returns are decoded elsewhere (a pointer-typed kInt64,
    // never a bare kNull CObject).
    @JvmStatic external fun postBytesToPort(dartPort: Long, value: ByteArray?)
    // Writes a thrown exception's name/message into the fresh-per-call
    // NitroError* whose address Dart passed as errPtr, before the trampoline
    // still posts exactly one message so the port fires either way.
    @JvmStatic external fun reportNativeAsyncError(errPtr: Long, name: String, message: String)

    // source: nitro_webgpu.native.dart:175
    @JvmStatic fun initInstance_call(instanceId: Long, options: ByteArray): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        val optionsBuf = java.nio.ByteBuffer.wrap(options).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        optionsBuf.getInt() // skip Dart 4-byte outer length prefix
        val optionsDecoded = GpuInstanceOptions.decodeFrom(optionsBuf)
        impl.initInstance(optionsDecoded)
    }
    // source: nitro_webgpu.native.dart:178
    @JvmStatic fun wgpuVersion_call(instanceId: Long): String {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        return impl.wgpuVersion()
    }
    // source: nitro_webgpu.native.dart:185
    @JvmStatic fun requestAdapter_call(instanceId: Long, options: ByteArray, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val optionsBuf = java.nio.ByteBuffer.wrap(options).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        optionsBuf.getInt() // skip Dart 4-byte outer length prefix
        val optionsDecoded = GpuRequestAdapterOptions.decodeFrom(optionsBuf)
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.requestAdapter(optionsDecoded) }
            postInt64ToPort(dartPort, result.toLong())
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_webgpu.native.dart:187
    @JvmStatic fun adapterGetInfo_call(instanceId: Long, adapter: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        val result = impl.adapterGetInfo(adapter)
        return result.encode()
    }
    // source: nitro_webgpu.native.dart:188
    @JvmStatic fun adapterGetLimits_call(instanceId: Long, adapter: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        val result = impl.adapterGetLimits(adapter)
        return result.encode()
    }
    // source: nitro_webgpu.native.dart:189
    @JvmStatic fun adapterRelease_call(instanceId: Long, adapter: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        impl.adapterRelease(adapter)
    }
    // source: nitro_webgpu.native.dart:196
    @JvmStatic fun requestDevice_call(instanceId: Long, adapter: Long, descriptor: ByteArray, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        val descriptorBuf = java.nio.ByteBuffer.wrap(descriptor).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        descriptorBuf.getInt() // skip Dart 4-byte outer length prefix
        val descriptorDecoded = GpuDeviceDescriptor.decodeFrom(descriptorBuf)
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.requestDevice(adapter, descriptorDecoded) }
            postInt64ToPort(dartPort, result.toLong())
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_webgpu.native.dart:199
    @JvmStatic fun deviceGetQueue_call(instanceId: Long, device: Long): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        return impl.deviceGetQueue(device)
    }
    // source: nitro_webgpu.native.dart:204
    @JvmStatic fun deviceDestroy_call(instanceId: Long, device: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        impl.deviceDestroy(device)
    }
    // source: nitro_webgpu.native.dart:206
    @JvmStatic fun deviceRelease_call(instanceId: Long, device: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        impl.deviceRelease(device)
    }
    // source: nitro_webgpu.native.dart:207
    @JvmStatic fun queueRelease_call(instanceId: Long, queue: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        impl.queueRelease(queue)
    }
    // source: nitro_webgpu.native.dart:213
    @JvmStatic fun devicePushErrorScope_call(instanceId: Long, device: Long, filter: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroWebgpu instance $instanceId not registered")
        impl.devicePushErrorScope(device, filter)
    }
    // source: nitro_webgpu.native.dart:217
    @JvmStatic fun devicePopErrorScope_call(instanceId: Long, device: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.devicePopErrorScope(device) }
            val _bytes = result?.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    private val _streamJobs = java.util.concurrent.ConcurrentHashMap<Pair<String, Long>, kotlinx.coroutines.Job>()

    @JvmStatic external fun emit_uncapturedErrors(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_webgpu_register_uncaptured_errors_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("uncapturedErrors", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.uncapturedErrors
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_uncapturedErrors(dartPort, item.encode())) {
                        _streamJobs.remove(Pair("uncapturedErrors", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_webgpu_release_uncaptured_errors_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("uncapturedErrors", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_deviceLostEvents(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_webgpu_register_device_lost_events_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("deviceLostEvents", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.deviceLostEvents
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_deviceLostEvents(dartPort, item.encode())) {
                        _streamJobs.remove(Pair("deviceLostEvents", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_webgpu_release_device_lost_events_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("deviceLostEvents", dartPort))?.cancel()
    }
}
