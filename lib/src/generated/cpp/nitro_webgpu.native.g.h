// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.5.12
// Generated from: nitro_webgpu.native.dart
//
// Abstract C++ interface for NitroWebgpu.
// Mirrors RN Nitro's HybridXxxSpec pattern — pure-virtual C++ with stdlib types.
// 1. Subclass HybridNitroWebgpu and implement all pure-virtual methods.
// 2. Register via nitro_webgpu_register_factory() (multi-instance) or
//    nitro_webgpu_register_impl() (single-instance legacy API).
#pragma once
#ifndef NITRO_WEBGPU_NATIVE_G_H
#define NITRO_WEBGPU_NATIVE_G_H

#include <stdint.h>
#include <stddef.h>
#include <cstring>
#include <functional>
#include <memory>
#include <optional>
#include <string>
#include <stdexcept>
#include <vector>
#include <variant>
#include "nitro_webgpu.bridge.g.h"

#if defined(_MSC_VER) && !defined(strdup)
#define strdup _strdup
#endif

/// Lightweight read-only view over a binary payload.
/// For @zeroCopy returns: return a malloc'd buffer — ownership transfers,
/// and the bridge frees it when Dart's view is GC'd (never a member/stack ptr).
struct NitroCppBuffer {
    const uint8_t* data;
    size_t size;
};

/// Binary encoder for @HybridRecord / @NitroVariant return values.
/// Mirrors RN Nitro's NitroRecordWriter — build then call toNative().
struct NitroRecordWriter {
    std::vector<uint8_t> _buf;
    NitroRecordWriter() { _buf.reserve(64); }
    void writeInt(int64_t v) { _buf.insert(_buf.end(), reinterpret_cast<const uint8_t*>(&v), reinterpret_cast<const uint8_t*>(&v) + 8); }
    void writeInt32(int32_t v) { _buf.insert(_buf.end(), reinterpret_cast<const uint8_t*>(&v), reinterpret_cast<const uint8_t*>(&v) + 4); }
    void writeInt8(int8_t v) { _buf.push_back(static_cast<uint8_t>(v)); }
    void writeDouble(double v) { _buf.insert(_buf.end(), reinterpret_cast<const uint8_t*>(&v), reinterpret_cast<const uint8_t*>(&v) + 8); }
    void writeBool(bool v) { _buf.push_back(v ? 1 : 0); }
    void writeString(const std::string& s) { int32_t n = (int32_t)s.size(); writeInt32(n); _buf.insert(_buf.end(), s.begin(), s.end()); }
    void writeBytes(const uint8_t* data, size_t n) { if (data && n) _buf.insert(_buf.end(), data, data + n); }
    /// Returns heap-allocated [4B length][payload]. Caller must ::free().
    uint8_t* toNative() const {
        int32_t payloadLen = (int32_t)_buf.size();
        uint8_t* out = (uint8_t*)::malloc(sizeof(int32_t) + (size_t)payloadLen);
        if (!out) return nullptr;
        ::memcpy(out, &payloadLen, sizeof(int32_t));
        if (payloadLen > 0) ::memcpy(out + sizeof(int32_t), _buf.data(), (size_t)payloadLen);
        return out;
    }
    /// Non-owning view of the payload (no length prefix). Valid while this writer lives.
    /// For encoding NESTED payloads only — never return it or pass it to an
    /// emit_* stream helper (both transfer ownership; use toNativeBuffer()).
    NitroCppBuffer toBuffer() const { return { _buf.data(), _buf.size() }; }
    /// Heap-allocated [4B length][payload] wrapped in a buffer whose size
    /// includes the prefix. Use as the return value of record/variant methods
    /// AND as the item passed to emit_* stream helpers: ownership transfers
    /// to Dart (freed after decoding via the <lib>_nitro_free export).
    NitroCppBuffer toNativeBuffer() const { return { toNative(), sizeof(int32_t) + _buf.size() }; }
};

// Bounds-checked binary reader for @HybridRecord wire payloads.
// Every readNullTag() bounds-checks before the byte access (IMPROVEMENTS.md §3.3).
struct NitroRecordReader {
  const uint8_t* _data;
  size_t         _size;
  size_t         _offset;
  explicit NitroRecordReader(NitroCppBuffer buf)
      : _data(buf.data), _size(buf.size), _offset(0) {}
  explicit NitroRecordReader(const uint8_t* data, size_t size)
      : _data(data), _size(size), _offset(0) {}
private:
  void _require(size_t n) const {
      if (_offset + n > _size)
          throw std::runtime_error("NitroRecordReader: buffer underflow");
  }
public:
  int64_t readInt() {
      _require(8); int64_t v; std::memcpy(&v, _data + _offset, 8); _offset += 8; return v;
  }
  int32_t readInt32() {
      _require(4); int32_t v; std::memcpy(&v, _data + _offset, 4); _offset += 4; return v;
  }
  int8_t readInt8() {
      _require(1); return static_cast<int8_t>(_data[_offset++]);
  }
  double readDouble() {
      _require(8); uint64_t b; std::memcpy(&b, _data + _offset, 8);
      _offset += 8; double v; std::memcpy(&v, &b, 8); return v;
  }
  bool readBool() {
      _require(1); return _data[_offset++] != 0;
  }
  std::string readString() {
      int32_t len = readInt32(); _require((size_t)len);
      std::string s(reinterpret_cast<const char*>(_data + _offset), (size_t)len);
      _offset += (size_t)len; return s;
  }
  // Bounds-checked null tag read — throws if no byte remains.
  bool readNullTag() {
      if (_offset + 1 > _size)
          throw std::runtime_error("NitroRecordReader: null tag read past end of buffer");
      return _data[_offset++] == 0;
  }
  // TypedData blob: [4B count][count bytes] → reads into caller-owned vector.
  void readBytes(uint8_t* dst, size_t n) {
      _require(n); std::memcpy(dst, _data + _offset, n); _offset += n;
  }
};
// @HybridRecord C++ structs (generated by Nitrogen)
struct GpuInstanceOptions;
struct GpuRequestAdapterOptions;
struct GpuAdapterInfo;
struct GpuLimits;
struct GpuDeviceDescriptor;
struct GpuError;
struct GpuUncapturedError;
struct GpuDeviceLost;
struct NitroNullableInt;
struct NitroNullableDouble;
struct NitroNullableBool;

struct GpuInstanceOptions {
    int64_t backends;

    static GpuInstanceOptions fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static GpuInstanceOptions fromReader(NitroRecordReader& _r) {
        GpuInstanceOptions _obj;
        _obj.backends = _r.readInt();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeInt(backends);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct GpuRequestAdapterOptions {
    int64_t powerPreference;
    bool forceFallbackAdapter;

    static GpuRequestAdapterOptions fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static GpuRequestAdapterOptions fromReader(NitroRecordReader& _r) {
        GpuRequestAdapterOptions _obj;
        _obj.powerPreference = _r.readInt();
        _obj.forceFallbackAdapter = _r.readBool();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeInt(powerPreference);
        w.writeBool(forceFallbackAdapter);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct GpuAdapterInfo {
    std::string vendor;
    std::string architecture;
    std::string device;
    std::string description;
    int64_t backendType;
    int64_t adapterType;

    static GpuAdapterInfo fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static GpuAdapterInfo fromReader(NitroRecordReader& _r) {
        GpuAdapterInfo _obj;
        _obj.vendor = _r.readString();
        _obj.architecture = _r.readString();
        _obj.device = _r.readString();
        _obj.description = _r.readString();
        _obj.backendType = _r.readInt();
        _obj.adapterType = _r.readInt();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeString(vendor);
        w.writeString(architecture);
        w.writeString(device);
        w.writeString(description);
        w.writeInt(backendType);
        w.writeInt(adapterType);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct GpuLimits {
    int64_t maxTextureDimension1D;
    int64_t maxTextureDimension2D;
    int64_t maxTextureDimension3D;
    int64_t maxTextureArrayLayers;
    int64_t maxBindGroups;
    int64_t maxBindingsPerBindGroup;
    int64_t maxUniformBufferBindingSize;
    int64_t maxStorageBufferBindingSize;
    int64_t minUniformBufferOffsetAlignment;
    int64_t minStorageBufferOffsetAlignment;
    int64_t maxBufferSize;
    int64_t maxComputeWorkgroupStorageSize;
    int64_t maxComputeInvocationsPerWorkgroup;
    int64_t maxComputeWorkgroupSizeX;
    int64_t maxComputeWorkgroupSizeY;
    int64_t maxComputeWorkgroupSizeZ;

    static GpuLimits fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static GpuLimits fromReader(NitroRecordReader& _r) {
        GpuLimits _obj;
        _obj.maxTextureDimension1D = _r.readInt();
        _obj.maxTextureDimension2D = _r.readInt();
        _obj.maxTextureDimension3D = _r.readInt();
        _obj.maxTextureArrayLayers = _r.readInt();
        _obj.maxBindGroups = _r.readInt();
        _obj.maxBindingsPerBindGroup = _r.readInt();
        _obj.maxUniformBufferBindingSize = _r.readInt();
        _obj.maxStorageBufferBindingSize = _r.readInt();
        _obj.minUniformBufferOffsetAlignment = _r.readInt();
        _obj.minStorageBufferOffsetAlignment = _r.readInt();
        _obj.maxBufferSize = _r.readInt();
        _obj.maxComputeWorkgroupStorageSize = _r.readInt();
        _obj.maxComputeInvocationsPerWorkgroup = _r.readInt();
        _obj.maxComputeWorkgroupSizeX = _r.readInt();
        _obj.maxComputeWorkgroupSizeY = _r.readInt();
        _obj.maxComputeWorkgroupSizeZ = _r.readInt();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeInt(maxTextureDimension1D);
        w.writeInt(maxTextureDimension2D);
        w.writeInt(maxTextureDimension3D);
        w.writeInt(maxTextureArrayLayers);
        w.writeInt(maxBindGroups);
        w.writeInt(maxBindingsPerBindGroup);
        w.writeInt(maxUniformBufferBindingSize);
        w.writeInt(maxStorageBufferBindingSize);
        w.writeInt(minUniformBufferOffsetAlignment);
        w.writeInt(minStorageBufferOffsetAlignment);
        w.writeInt(maxBufferSize);
        w.writeInt(maxComputeWorkgroupStorageSize);
        w.writeInt(maxComputeInvocationsPerWorkgroup);
        w.writeInt(maxComputeWorkgroupSizeX);
        w.writeInt(maxComputeWorkgroupSizeY);
        w.writeInt(maxComputeWorkgroupSizeZ);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct GpuDeviceDescriptor {
    std::string label;

    static GpuDeviceDescriptor fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static GpuDeviceDescriptor fromReader(NitroRecordReader& _r) {
        GpuDeviceDescriptor _obj;
        _obj.label = _r.readString();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeString(label);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct GpuError {
    int64_t type;
    std::string message;

    static GpuError fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static GpuError fromReader(NitroRecordReader& _r) {
        GpuError _obj;
        _obj.type = _r.readInt();
        _obj.message = _r.readString();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeInt(type);
        w.writeString(message);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct GpuUncapturedError {
    int64_t deviceAddress;
    int64_t type;
    std::string message;

    static GpuUncapturedError fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static GpuUncapturedError fromReader(NitroRecordReader& _r) {
        GpuUncapturedError _obj;
        _obj.deviceAddress = _r.readInt();
        _obj.type = _r.readInt();
        _obj.message = _r.readString();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeInt(deviceAddress);
        w.writeInt(type);
        w.writeString(message);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct GpuDeviceLost {
    int64_t deviceAddress;
    int64_t reason;
    std::string message;

    static GpuDeviceLost fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static GpuDeviceLost fromReader(NitroRecordReader& _r) {
        GpuDeviceLost _obj;
        _obj.deviceAddress = _r.readInt();
        _obj.reason = _r.readInt();
        _obj.message = _r.readString();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeInt(deviceAddress);
        w.writeInt(reason);
        w.writeString(message);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct NitroNullableInt {
    bool hasValue;
    int64_t value;

    static NitroNullableInt fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static NitroNullableInt fromReader(NitroRecordReader& _r) {
        NitroNullableInt _obj;
        _obj.hasValue = _r.readBool();
        _obj.value = _r.readInt();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeBool(hasValue);
        w.writeInt(value);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct NitroNullableDouble {
    bool hasValue;
    double value;

    static NitroNullableDouble fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static NitroNullableDouble fromReader(NitroRecordReader& _r) {
        NitroNullableDouble _obj;
        _obj.hasValue = _r.readBool();
        _obj.value = _r.readDouble();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeBool(hasValue);
        w.writeDouble(value);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

struct NitroNullableBool {
    bool hasValue;
    bool value;

    static NitroNullableBool fromNative(NitroCppBuffer buf) {
        NitroRecordReader _r(buf);
        return fromReader(_r);
    }

    static NitroNullableBool fromReader(NitroRecordReader& _r) {
        NitroNullableBool _obj;
        _obj.hasValue = _r.readBool();
        _obj.value = _r.readBool();
        return _obj;
    }

    // Mirrors fromReader — appends this record's fields to [w].
    void encodeInto(NitroRecordWriter& w) const {
        w.writeBool(hasValue);
        w.writeBool(value);
    }

    /// Heap-allocated [4B length][payload] for returning to Dart.
    NitroCppBuffer toNativeBuffer() const {
        NitroRecordWriter _w;
        encodeInto(_w);
        return _w.toNativeBuffer();
    }
};

/// Abstract C++ interface for NitroWebgpu.
/// 1. Subclass this and implement all pure-virtual methods.
/// 2. Call nitro_webgpu_register_impl(&myInstance) at startup.
/// 3. Call nitro_webgpu_register_impl(nullptr) in teardown.
class HybridNitroWebgpu {
public:
    virtual ~HybridNitroWebgpu() = default;

    // ── Methods ──────────────────────────────────────────────────────────
    // source: nitro_webgpu.native.dart:175
    virtual void initInstance(NitroCppBuffer options) = 0;
    // source: nitro_webgpu.native.dart:178
    virtual std::string wgpuVersion() = 0;
    // source: nitro_webgpu.native.dart:185
    virtual void requestAdapter(NitroCppBuffer options, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_webgpu.native.dart:187
    virtual NitroCppBuffer adapterGetInfo(int64_t adapter) = 0;
    // source: nitro_webgpu.native.dart:188
    virtual NitroCppBuffer adapterGetLimits(int64_t adapter) = 0;
    // source: nitro_webgpu.native.dart:189
    virtual void adapterRelease(int64_t adapter) = 0;
    // source: nitro_webgpu.native.dart:196
    virtual void requestDevice(int64_t adapter, NitroCppBuffer descriptor, NitroError* _nitro_err, int64_t dartPort) = 0;
    // source: nitro_webgpu.native.dart:199
    virtual int64_t deviceGetQueue(int64_t device) = 0;
    // source: nitro_webgpu.native.dart:204
    virtual void deviceDestroy(int64_t device) = 0;
    // source: nitro_webgpu.native.dart:206
    virtual void deviceRelease(int64_t device) = 0;
    // source: nitro_webgpu.native.dart:207
    virtual void queueRelease(int64_t queue) = 0;
    // source: nitro_webgpu.native.dart:213
    virtual void devicePushErrorScope(int64_t device, int64_t filter) = 0;
    // source: nitro_webgpu.native.dart:217
    virtual void devicePopErrorScope(int64_t device, NitroError* _nitro_err, int64_t dartPort) = 0;

    // ── Streams ──────────────────────────────────────────────────────────
    // Call the emit_* helpers below to push items to Dart from any thread.
    /// Emit a value on the uncapturedErrors stream.
    /// Pass record.toNativeBuffer() (or nitro_Xxx_to_native(...)) — a heap
    /// [4B len][payload] block. Ownership transfers: the bridge/Dart frees
    /// it. Never pass a non-owning writer.toBuffer() view.
    void emit_uncapturedErrors(NitroCppBuffer item);
    /// Emit a value on the deviceLostEvents stream.
    /// Pass record.toNativeBuffer() (or nitro_Xxx_to_native(...)) — a heap
    /// [4B len][payload] block. Ownership transfers: the bridge/Dart frees
    /// it. Never pass a non-owning writer.toBuffer() view.
    void emit_deviceLostEvents(NitroCppBuffer item);

protected:
    HybridNitroWebgpu() = default;
};

#ifdef __cplusplus
/// Factory function type — mirrors RN Nitro's HybridObjectRegistry constructor fn.
/// Takes the instance key (from Dart's getInstance(key)) and returns a shared_ptr.
using HybridNitroWebgpuFactory = std::function<std::shared_ptr<HybridNitroWebgpu>(const std::string&)>;
#endif

#ifdef __cplusplus
extern "C" {
#endif

/// Register a factory function (recommended — enables multiple instances).
/// Called from create_instance() each time Dart requests a new object.
void nitro_webgpu_register_factory(void* factory_fn_ptr);

/// Register a single raw-pointer implementation (legacy single-instance API).
/// Use register_factory() instead for new code.
void nitro_webgpu_register_impl(HybridNitroWebgpu* impl);

/// Return the registered raw-pointer impl, or nullptr (legacy API).
HybridNitroWebgpu* nitro_webgpu_get_impl(void);

#ifdef __cplusplus
} // end extern "C"

/// C++-only typed factory registration — preferred over the C void* variant.
inline void nitro_webgpu_register_factory_typed(HybridNitroWebgpuFactory factory) {
    nitro_webgpu_register_factory(static_cast<void*>(&factory));
}
#endif

#endif // NITRO_WEBGPU_NATIVE_G_H
