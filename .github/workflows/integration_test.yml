name: integration tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  macos:
    name: macOS (Metal)
    runs-on: macos-15
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          # Pinned to the version the plugin is developed/verified against —
          # newer stable's integration-test result drain undercounts on fast
          # suites (all tests pass in-app, tool exits 1).
          flutter-version: 3.44.4
          cache: true
      - name: Cache wgpu-native
        uses: actions/cache@v4
        with:
          path: |
            src/third_party/wgpu_native
            macos/nitro_webgpu/Frameworks
            ios/nitro_webgpu/Frameworks
          key: wgpu-native-${{ runner.os }}-${{ hashFiles('scripts/wgpu_native.sha256') }}
      - name: Fetch wgpu-native
        run: ./scripts/fetch_wgpu_native.sh --targets macos-aarch64,macos-x86_64,ios-aarch64,ios-aarch64-simulator,ios-x86_64-simulator
      - name: Release build (symbol survival gate)
        working-directory: example
        run: |
          flutter pub get
          flutter build macos --release
      - name: Integration tests
        working-directory: example
        run: flutter test integration_test/wgpu_instance_test.dart -d macos --reporter expanded
      - name: Complex parity tests
        working-directory: example
        run: flutter test integration_test/wgpu_parity_complex_test.dart -d macos --reporter expanded
      - name: Robustness tests
        working-directory: example
        run: flutter test integration_test/wgpu_robustness_test.dart -d macos --reporter expanded
      - name: Showcase tests
        working-directory: example
        run: flutter test integration_test/wgpu_showcase_test.dart -d macos --reporter expanded

  # Dawn backend (feat/dawn-backend): same four suites on Google's WebGPU
  # implementation. Dawn is built from source once and cached; a cache miss
  # costs ~30 min, hits are seconds.
  dawn-macos:
    name: macOS (Dawn)
    runs-on: macos-15
    timeout-minutes: 90
    continue-on-error: true  # experimental lane while the migration bakes
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.4
          cache: true
      - name: Install glslang
        run: brew install glslang
      - name: Cache Dawn build
        id: dawn-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/nitro_webgpu/dawn-src
          key: dawn-macos-${{ hashFiles('scripts/stage_dawn_macos.sh') }}-v1
      - name: Build Dawn
        if: steps.dawn-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/.cache/nitro_webgpu
          git clone --depth 1 https://github.com/google/dawn.git ~/.cache/nitro_webgpu/dawn-src
          cmake -S ~/.cache/nitro_webgpu/dawn-src -B ~/.cache/nitro_webgpu/dawn-src/out \
            -DCMAKE_BUILD_TYPE=Release -DDAWN_FETCH_DEPENDENCIES=ON \
            -DDAWN_BUILD_MONOLITHIC_LIBRARY=SHARED -DTINT_BUILD_SPV_READER=ON \
            -DDAWN_BUILD_SAMPLES=OFF -DDAWN_USE_GLFW=OFF \
            -DTINT_BUILD_TESTS=OFF -DTINT_BUILD_CMD_TOOLS=OFF
          cmake --build ~/.cache/nitro_webgpu/dawn-src/out --target webgpu_dawn -j 3
      - name: Switch to the Dawn backend
        run: ./scripts/set_backend_macos.sh dawn
      - name: Integration tests
        working-directory: example
        run: flutter test integration_test/wgpu_instance_test.dart -d macos --reporter expanded
      - name: Complex parity tests
        working-directory: example
        run: flutter test integration_test/wgpu_parity_complex_test.dart -d macos --reporter expanded
      - name: Robustness tests
        working-directory: example
        run: flutter test integration_test/wgpu_robustness_test.dart -d macos --reporter expanded
      - name: Showcase tests
        working-directory: example
        run: flutter test integration_test/wgpu_showcase_test.dart -d macos --reporter expanded

  # Dawn on Windows — proves the DXGI shared-handle import path on real
  # D3D12/WARP. Dawn source build is cached (first run ~50 min, hits are
  # seconds). GLSL tests probe-skip (no glslang staged on this lane).
  dawn-windows:
    name: Windows (Dawn)
    runs-on: windows-latest
    timeout-minutes: 120
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.4
          cache: true
      - name: Cache Dawn build
        id: dawn-cache
        uses: actions/cache@v4
        with:
          path: dawn-src
          key: dawn-windows-${{ hashFiles('scripts/stage_dawn_macos.sh') }}-v1
      - name: Build Dawn
        if: steps.dawn-cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          git clone --depth 1 https://github.com/google/dawn.git dawn-src
          cmake -S dawn-src -B dawn-src/out -DCMAKE_BUILD_TYPE=Release             -DDAWN_FETCH_DEPENDENCIES=ON -DDAWN_BUILD_MONOLITHIC_LIBRARY=SHARED             -DTINT_BUILD_SPV_READER=ON -DDAWN_BUILD_SAMPLES=OFF             -DDAWN_USE_GLFW=OFF -DTINT_BUILD_TESTS=OFF -DTINT_BUILD_CMD_TOOLS=OFF
          cmake --build dawn-src/out --config Release --target webgpu_dawn -j 4
      - name: Stage Dawn
        shell: bash
        run: |
          mkdir -p src/third_party/dawn/windows-x86_64/lib                    src/third_party/dawn/include/webgpu src/third_party/dawn/include/dawn
          cp dawn-src/include/webgpu/webgpu.h src/third_party/dawn/include/webgpu/
          cp dawn-src/out/gen/include/dawn/webgpu.h src/third_party/dawn/include/dawn/
          find dawn-src/out -name "webgpu_dawn.lib" -exec cp {} src/third_party/dawn/windows-x86_64/lib/ \;
          find dawn-src/out -name "webgpu_dawn.dll" -exec cp {} src/third_party/dawn/windows-x86_64/lib/ \;
          printf 'dawn' > src/third_party/BACKEND
      - name: Integration tests
        working-directory: example
        run: flutter test integration_test/wgpu_instance_test.dart -d windows --reporter expanded
      - name: Complex parity tests
        working-directory: example
        run: flutter test integration_test/wgpu_parity_complex_test.dart -d windows --reporter expanded
      - name: Robustness tests
        working-directory: example
        run: flutter test integration_test/wgpu_robustness_test.dart -d windows --reporter expanded
      - name: Showcase tests
        working-directory: example
        run: flutter test integration_test/wgpu_showcase_test.dart -d windows --reporter expanded

  # Dawn on Linux — Vulkan over lavapipe, readback presentation (the GTK
  # embedder has no dmabuf consumer).
  dawn-linux:
    name: Linux (Dawn)
    runs-on: ubuntu-latest
    timeout-minutes: 120
    continue-on-error: true
    env:
      LIBGL_ALWAYS_SOFTWARE: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.4
          cache: true
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build clang cmake pkg-config libgtk-3-dev             mesa-vulkan-drivers vulkan-tools xvfb
      - name: Cache Dawn build
        id: dawn-cache
        uses: actions/cache@v4
        with:
          path: dawn-src
          key: dawn-linux-${{ hashFiles('scripts/stage_dawn_macos.sh') }}-v1
      - name: Build Dawn
        if: steps.dawn-cache.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 https://github.com/google/dawn.git dawn-src
          cmake -S dawn-src -B dawn-src/out -GNinja -DCMAKE_BUILD_TYPE=Release             -DDAWN_FETCH_DEPENDENCIES=ON -DDAWN_BUILD_MONOLITHIC_LIBRARY=SHARED             -DTINT_BUILD_SPV_READER=ON -DDAWN_BUILD_SAMPLES=OFF             -DDAWN_USE_GLFW=OFF -DTINT_BUILD_TESTS=OFF -DTINT_BUILD_CMD_TOOLS=OFF
          cmake --build dawn-src/out --target webgpu_dawn -j 4
      - name: Stage Dawn
        run: |
          mkdir -p src/third_party/dawn/linux-x86_64/lib                    src/third_party/dawn/include/webgpu src/third_party/dawn/include/dawn
          cp dawn-src/include/webgpu/webgpu.h src/third_party/dawn/include/webgpu/
          cp dawn-src/out/gen/include/dawn/webgpu.h src/third_party/dawn/include/dawn/
          cp dawn-src/out/src/dawn/native/libwebgpu_dawn.so src/third_party/dawn/linux-x86_64/lib/
          printf 'dawn' > src/third_party/BACKEND
      - name: Integration tests
        working-directory: example
        run: |
          ulimit -n 8192
          xvfb-run -a flutter test integration_test/wgpu_instance_test.dart -d linux --reporter expanded
      - name: Complex parity tests
        working-directory: example
        run: xvfb-run -a flutter test integration_test/wgpu_parity_complex_test.dart -d linux --reporter expanded
      - name: Robustness tests
        working-directory: example
        run: xvfb-run -a flutter test integration_test/wgpu_robustness_test.dart -d linux --reporter expanded
      - name: Showcase tests
        working-directory: example
        run: xvfb-run -a flutter test integration_test/wgpu_showcase_test.dart -d linux --reporter expanded

  ios:
    name: iOS (simulator, Metal)
    # macos-15 runners have an ongoing iOS-simulator failure-to-start/hang
    # issue (actions/runner-images#12777) — the community-verified
    # workaround is macos-14, available until the November deprecation
    # (actions/runner-images#13518). continue-on-error keeps deprecation
    # brownouts and residual simulator flakiness from blocking the board;
    # drop both once #12777 is fixed on macos-15.
    runs-on: macos-14
    continue-on-error: true
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          # Pinned to the version the plugin is developed/verified against —
          # newer stable's integration-test result drain undercounts on fast
          # suites (all tests pass in-app, tool exits 1).
          flutter-version: 3.44.4
          cache: true
      - name: Cache wgpu-native
        uses: actions/cache@v4
        with:
          path: |
            src/third_party/wgpu_native
            macos/nitro_webgpu/Frameworks
            ios/nitro_webgpu/Frameworks
          key: wgpu-native-${{ runner.os }}-${{ hashFiles('scripts/wgpu_native.sha256') }}
      - name: Fetch wgpu-native
        run: ./scripts/fetch_wgpu_native.sh --targets macos-aarch64,macos-x86_64,ios-aarch64,ios-aarch64-simulator,ios-x86_64-simulator
      - name: Boot iOS simulator
        run: |
          UDID=$(xcrun simctl list devices available --json \
            | jq -r '[.devices | to_entries[] | select(.key | contains("iOS")) | .value[] | select(.name | test("^iPhone"))][0].udid')
          if [ -z "$UDID" ] || [ "$UDID" = "null" ]; then
            echo "no available iPhone simulator"; xcrun simctl list devices; exit 1
          fi
          xcrun simctl boot "$UDID"
          xcrun simctl bootstatus "$UDID" -b
          # simctl boot alone leaves launch services half-up on CI runners —
          # the flutter tool then hangs forever installing/attaching after
          # "Xcode build done". The Simulator app process brings them up.
          open -a Simulator --args -CurrentDeviceUDID "$UDID"
          sleep 20
          echo "SIM_UDID=$UDID" >> "$GITHUB_ENV"
      - name: Integration tests
        working-directory: example
        run: |
          flutter pub get
          flutter test integration_test/wgpu_instance_test.dart -d "$SIM_UDID" --reporter expanded
      - name: Complex parity tests
        working-directory: example
        run: flutter test integration_test/wgpu_parity_complex_test.dart -d "$SIM_UDID" --reporter expanded
      - name: Robustness tests
        working-directory: example
        run: flutter test integration_test/wgpu_robustness_test.dart -d "$SIM_UDID" --reporter expanded
      - name: Showcase tests
        working-directory: example
        run: flutter test integration_test/wgpu_showcase_test.dart -d "$SIM_UDID" --reporter expanded
      - name: Hang diagnostics (simulator + app logs)
        if: failure()
        run: |
          echo "=== installed apps ==="
          xcrun simctl listapps "$SIM_UDID" 2>/dev/null | head -40 || true
          echo "=== recent simulator log (errors/faults) ==="
          xcrun simctl spawn "$SIM_UDID" log show --last 10m --predicate \
            'messageType == error OR messageType == fault' 2>/dev/null | tail -80 || true
          echo "=== recent app log lines ==="
          xcrun simctl spawn "$SIM_UDID" log show --last 10m --predicate \
            'process CONTAINS "Runner" OR process CONTAINS "nitro"' 2>/dev/null | tail -60 || true

  android:
    name: Android (emulator, SwiftShader Vulkan)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: subosito/flutter-action@v2
        with:
          # Pinned to the version the plugin is developed/verified against —
          # newer stable's integration-test result drain undercounts on fast
          # suites (all tests pass in-app, tool exits 1).
          flutter-version: 3.44.4
          cache: true
      - name: Cache wgpu-native
        uses: actions/cache@v4
        with:
          path: src/third_party/wgpu_native
          key: wgpu-native-android-${{ hashFiles('scripts/wgpu_native.sha256') }}
      - name: Fetch wgpu-native
        run: ./scripts/fetch_wgpu_native.sh --targets android-aarch64,android-armv7,android-x86_64
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Integration tests on emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          target: google_apis
          profile: pixel_6
          emulator-options: -no-snapshot -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          disable-animations: true
          working-directory: example
          script: |
            flutter pub get
            flutter test integration_test/wgpu_instance_test.dart -d emulator-5554 --reporter expanded
            flutter test integration_test/wgpu_parity_complex_test.dart -d emulator-5554 --reporter expanded
            flutter test integration_test/wgpu_robustness_test.dart -d emulator-5554 --reporter expanded
            flutter test integration_test/wgpu_showcase_test.dart -d emulator-5554 --reporter expanded

  linux:
    name: Linux (lavapipe software Vulkan)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          # Pinned to the version the plugin is developed/verified against —
          # newer stable's integration-test result drain undercounts on fast
          # suites (all tests pass in-app, tool exits 1).
          flutter-version: 3.44.4
          cache: true
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev mesa-vulkan-drivers mesa-utils libgl1-mesa-dri libegl1 vulkan-tools xvfb
      - name: Cache wgpu-native
        uses: actions/cache@v4
        with:
          path: src/third_party/wgpu_native
          key: wgpu-native-${{ runner.os }}-${{ hashFiles('scripts/wgpu_native.sha256') }}
      - name: Fetch wgpu-native
        run: ./scripts/fetch_wgpu_native.sh --targets linux-x86_64
      - name: Release build (symbol survival gate)
        working-directory: example
        run: |
          flutter pub get
          flutter build linux --release
      - name: Integration tests (fallback adapter)
        working-directory: example
        # LIBGL_ALWAYS_SOFTWARE: the GTK embedder needs working GL for the
        # raster thread; under Xvfb that must be Mesa llvmpipe, not a GPU
        # probe that can abort the app at first frame.
        # ulimit -n: every lavapipe VkDevice costs fds + a thread pool; the
        # suite creates a device per test and the runner default (1024) runs
        # out mid-suite — wgpuInstanceRequestAdapter then panics (core-dump
        # verified).
        env:
          LIBGL_ALWAYS_SOFTWARE: "1"
        run: |
          ulimit -n 8192
          xvfb-run -a -s "-screen 0 1280x800x24" flutter test integration_test/wgpu_instance_test.dart -d linux --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded
      - name: Complex parity tests (fallback adapter)
        working-directory: example
        env:
          LIBGL_ALWAYS_SOFTWARE: "1"
        run: |
          ulimit -n 8192
          xvfb-run -a -s "-screen 0 1280x800x24" flutter test integration_test/wgpu_parity_complex_test.dart -d linux --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded
      - name: Robustness tests (fallback adapter)
        working-directory: example
        env:
          LIBGL_ALWAYS_SOFTWARE: "1"
        run: |
          ulimit -n 8192
          xvfb-run -a -s "-screen 0 1280x800x24" flutter test integration_test/wgpu_robustness_test.dart -d linux --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded
      - name: Showcase tests (fallback adapter)
        working-directory: example
        env:
          LIBGL_ALWAYS_SOFTWARE: "1"
        run: |
          ulimit -n 8192
          xvfb-run -a -s "-screen 0 1280x800x24" flutter test integration_test/wgpu_showcase_test.dart -d linux --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded
      - name: Crash diagnostics (core-dump backtrace + app stderr)
        if: failure()
        working-directory: example
        env:
          LIBGL_ALWAYS_SOFTWARE: "1"
        run: |
          sudo apt-get install -y gdb
          sudo sysctl -w kernel.core_pattern=/tmp/core.%p
          ulimit -c unlimited
          # Wrap the app binary so its stderr (Rust panic messages — the
          # flutter tool discards child stderr) lands in a file we can print.
          # `flutter test` reinstalls the bundle at the start of every
          # invocation, so a background watcher re-wraps the fresh binary in
          # the build→launch gap.
          BUNDLE=build/linux/x64/debug/bundle
          wrap_binary() {
            if [ -f "$BUNDLE/nitro_webgpu_example" ] \
              && ! head -c2 "$BUNDLE/nitro_webgpu_example" | grep -q '#!'; then
              mv "$BUNDLE/nitro_webgpu_example" "$BUNDLE/nitro_webgpu_example.real"
              cat > "$BUNDLE/nitro_webgpu_example" <<'WRAP'
          #!/bin/bash
          exec "$(dirname "$0")/nitro_webgpu_example.real" "$@" 2>>/tmp/app_stderr.log
          WRAP
              chmod +x "$BUNDLE/nitro_webgpu_example"
              echo "(re)wrapped app binary"
            fi
          }
          wrap_binary
          ( while true; do wrap_binary; sleep 0.3; done ) &
          WATCHER=$!
          xvfb-run -a -s "-screen 0 1280x800x24" flutter test integration_test/wgpu_instance_test.dart -d linux \
            --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded || true
          kill $WATCHER 2>/dev/null || true
          echo "=== app stderr ==="
          tail -80 /tmp/app_stderr.log 2>/dev/null || echo "(no stderr captured)"
          echo "=== standalone adapter-cycle probe (wgpu panic goes to visible stderr) ==="
          cc ../scripts/ci/adapter_cycle_probe.c \
            ../src/third_party/wgpu_native/linux-x86_64/lib/libwgpu_native.a \
            -I../src/third_party/wgpu_native/include -lm -ldl -lpthread \
            -o /tmp/adapter_cycle_probe
          /tmp/adapter_cycle_probe 2>&1 | tail -8 || echo "PROBE CRASHED (exit $?)"
          shopt -s nullglob
          cores=(/tmp/core.*)
          if [ ${#cores[@]} -eq 0 ]; then echo "NO CORE PRODUCED"; exit 0; fi
          for c in "${cores[@]}"; do
            echo "=== crashing-thread backtrace for $c ==="
            gdb -batch -ex "bt" \
              build/linux/x64/debug/bundle/nitro_webgpu_example "$c" 2>/dev/null | tail -60
            echo "=== all threads (nitro/wgpu frames only) for $c ==="
            gdb -batch -ex "thread apply all bt" \
              build/linux/x64/debug/bundle/nitro_webgpu_example "$c" 2>/dev/null \
              | grep -B2 -A8 -iE "nwp_|nitro|wgpu|HybridNitro" | head -200
          done

  windows:
    name: Windows (D3D12 WARP)
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          # Pinned to the version the plugin is developed/verified against —
          # newer stable's integration-test result drain undercounts on fast
          # suites (all tests pass in-app, tool exits 1).
          flutter-version: 3.44.4
          cache: true
      - name: Cache wgpu-native
        uses: actions/cache@v4
        with:
          path: src/third_party/wgpu_native
          key: wgpu-native-${{ runner.os }}-${{ hashFiles('scripts/wgpu_native.sha256') }}
      - name: Fetch wgpu-native
        shell: bash
        run: ./scripts/fetch_wgpu_native.sh --targets windows-x86_64-msvc
      - name: Release build (symbol survival gate)
        working-directory: example
        run: |
          flutter pub get
          flutter build windows --release
      - name: Integration tests (fallback adapter)
        working-directory: example
        run: flutter test integration_test/wgpu_instance_test.dart -d windows --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded
      - name: Complex parity tests (fallback adapter)
        working-directory: example
        run: flutter test integration_test/wgpu_parity_complex_test.dart -d windows --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded
      - name: Robustness tests (fallback adapter)
        working-directory: example
        run: flutter test integration_test/wgpu_robustness_test.dart -d windows --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded
      - name: Showcase tests (fallback adapter)
        working-directory: example
        run: flutter test integration_test/wgpu_showcase_test.dart -d windows --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded
      - name: Crash diagnostics (isolate read-only depth test)
        if: failure()
        working-directory: example
        shell: bash
        run: |
          flutter test integration_test/wgpu_parity_complex_test.dart -d windows \
            --dart-define=WGPU_FORCE_FALLBACK=true --reporter expanded \
            --plain-name "read-only depth attachment still depth-tests draws" 2>&1 | tail -60
